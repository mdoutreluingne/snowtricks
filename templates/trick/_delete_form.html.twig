{% if btn_delete is not defined %}
<a href="{{ path('trick_delete', {'slug': trick.slug}) }}" class="btn" data-toggle="tooltip" data-placement="right"
        title="Supprimer" onclick="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément ?')">
<i class="fas fa-trash-alt fa-lg"></i>
</a>
{% else %}
<a href="{{ path('trick_delete', {'slug': trick.slug}) }}" class="btn btn-danger center-block"
        onclick="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément ?')">Supprimer</a>
{% endif %}